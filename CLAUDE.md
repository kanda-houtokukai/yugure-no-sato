# CLAUDE.md — 夕暮れの里（yugure-no-sato）

> Claude Code はセッション開始時にこのファイルを自動で読む。

## この案件について

- 案件名: 夕暮れの里（yugure-no-sato）
- 種別: 個人開発（実験）
- 台帳: `docs/yugure-no-sato-handoff.md`
- 決定事項の正本: `docs/decisions.md`

夏の夕暮れの、昔の日本の田舎の集落を散策するブラウザ体験。素の WebGPU（WGSL）だけで、
素材ファイルをリポジトリに一切持たずに世界を生成する。参照元は「Claude Code＋Opus 5 で
9時間・約400万トークンで雪原シミュレータを1本作った」事例で、同じ土俵で挑む実験。

## セッション開始時に必ずやること

1. `docs/yugure-no-sato-handoff.md` を読み、現在地・決定事項・教訓を把握する
2. `docs/decisions.md`（決定事項の正本）を読む
3. 作業規範は dev-workflow スキル（~/.claude/skills/dev-workflow/）に従う
4. 把握が終わるまで、実装・編集・デプロイをしない（鉄則1）

## この案件の絶対規則

### 1. 素材ゼロの原則
テクスチャ・3Dモデル・画像・音源ファイルを **一切リポジトリに入れない**。
すべて起動時に数式で生成する。音（ひぐらし・風・水音）も持ち込まず合成する。
建物・鳥居は数値（柱間隔・屋根勾配・板枚数等）から形を起こすパラメトリック生成とする。
- 判定基準: `git ls-files` に画像・音声・3Dモデルの拡張子が1つでも現れたら違反。
- 例外を作りたくなったら、実装せず**停止して報告**する。

### 2. 外部ライブラリ禁止
three.js 等の 3D／描画ライブラリを使わない。**素の WebGPU（WGSL）＋ TypeScript**。
ビルドツールは **Vite のみ許可**（開発依存として）。
- 描画・数学・物理・音声・ノイズ生成のライブラリはすべて禁止。必要なら自分で書く。
- 判定基準: `dependencies` は**空のまま保つ**。`devDependencies` は現在
  **vite / typescript / @webgpu/types / playwright の4点のみ**。追加したくなったら**停止して報告**する。
- `npm install` はグローバル設定の deny で止まる。勝手に外さず、コマンドを提示して神田さんに実行してもらう。

### 3. 仕様に無い選択を迫られたら、実装せず報告して停止する
決定事項リスト（`docs/decisions.md`）に書かれていない設計判断が必要になったら、
勝手に決めない。選択肢と推奨を添えて報告し、指示を待つ。

### 4. 各フェーズの区切りで push して SHA を報告する
設計側は raw（`raw.githubusercontent.com/kanda-houtokukai/yugure-no-sato/main/...`）で
進捗を裏取りする。**push 済みでないものは存在しないものとして扱われる**。
区切りごとに commit + push し、「push した」と SHA を明示報告する。

## 検証（自己修正ループ）— 描画を触ったら必ず通す

```
npm run verify     # 3視点のスクショ＋自己点検レポート。異常があれば終了コード非0
npm run typecheck  # src 配下の型検査
npm run probe      # WebGPU の取得可否を再実証（Chrome 更新時・別マシン）
```

- **撮った絵は必ず自分で Read して目視判定する。** レポートが全 ○ でも、絵が意図と違うことはある。
  レポートは「崩れの検出」であって「意図どおりかの判定」ではない。
- ゴールデンビューは**バイト単位で再現する**（実測済み。ヘッドレスと画面付きも一致）。
  スクショの sha256 が前回と変われば描画が変わったということ。回帰検出に使う。
- 出力は `.screenshots/`（gitignore 済み）。リポジトリには入れない。
- **症状から推測して直す前に、観測する。** フェーズ0で、成功している POST が
  `net::ERR_ABORTED` と報告される件を「順序の問題」と読んで直し、外した。
  要求の一生をログに出して初めて真因（本文の無い 204 応答）が見えた。

## 台帳の維持（肥大させない）

- 台帳は**毎セッションが起動時に読む**。肥大するとコンテキストを毎回浪費する。
- **版/フェーズの記録は直近 10 件まで。** 11 件目を足す前に、最も古い1件を
  `docs/yugure-no-sato-archive-YYYY-MM.md` へ**原文のまま**移してから追記する。
- **台帳が 3 万文字を超えたら**、まず記録の件数を数える:
  - 規定数（10件）以内 → 規則は守られている。重い回が続いただけ。何もしない
  - 規定数超過 → 超過分だけをアーカイブへ移す
- **チェックの引き金は「版/フェーズを追記するとき」**。追記の直前に必ず数える。
- ⚠️ **要約・圧縮による削減は禁止**（アーカイブは原文保存が目的）。
- 移す前に、その中の**いまも効いている制約**（確定値・禁則・落とし穴）を
  現在地サマリの「生きている注意事項」へ昇格させる。迷ったら残す側に倒す。
- 現在地サマリは常に **1〜2画面以内**に保つ。

## このリポジトリ固有の注意

- 公開範囲: **GitHub 公開リポジトリ・MIT**。設計側が raw で裏取りするため public を維持する。
  private 化しない。
- デプロイ方法: GitHub Pages（フェーズ1以降で設定する。現時点では未設定）。
- 検証環境: Google Chrome 安定版（macOS）。**WebGPU は起動フラグ不要・ヘッドレスでも動くことを実証済み**
  （2026-09-06）。Playwright は `channel: 'chrome'` でこの Chrome を起動する（同梱 Chromium は未導入）。
- モデル運用: 準備・調査フェーズは Opus、長時間の自律実装フェーズは Fable 5.1 に切り替える。
  切り替えの判断は dev-workflow §2 に従い、**作業を始める前に**ユーザーへ提案する。
- 検証ツール自身の制約で判定できないものは「**未検証**」として扱い、
  どのフェーズで解消するかを添えて台帳に繰り越す（「たぶん動いている」を残さない）。
